PORT=8080
# Comma-separated web origins. Leave unset only for non-browser clients.
CORS_ORIGIN=
# Use a secret manager in hosted environments; never commit the JSON key.
GOOGLE_APPLICATION_CREDENTIALS=
